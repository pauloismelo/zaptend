# Database Package

Este pacote contém o schema Prisma e as configurações de banco de dados compartilhadas.

- O schema deve ser mantido em `prisma/schema.prisma`.
- Use `pnpm --filter=database migrate:dev` para criar migrations.
