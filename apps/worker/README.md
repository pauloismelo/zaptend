# ZapTend Worker (NestJS + BullMQ)

Worker para processamento de filas (mensagens, notificações, billing) usando NestJS e BullMQ.

- NestJS
- BullMQ

Execute `pnpm dev --filter=worker` para rodar localmente.
