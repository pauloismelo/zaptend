# ZapTend API (NestJS)

Backend REST + WebSocket usando NestJS, Prisma, Swagger e class-validator.

- NestJS
- Prisma ORM
- Swagger (documentação automática)
- class-validator para validação de DTOs

Execute `pnpm dev --filter=api` para rodar localmente.
